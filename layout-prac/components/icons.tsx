import { LucideProps } from "lucide-react";
import { CircleUser } from "lucide-react"; // or use any icon you prefer

export const Icons = {
  logo: (props: LucideProps) => <CircleUser {...props} />,
};

export default Icons;